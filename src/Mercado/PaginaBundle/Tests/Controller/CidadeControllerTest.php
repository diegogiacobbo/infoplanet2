<?php

namespace Mercado\PaginaBundle\Tests\Controller;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

class CidadeControllerTest extends WebTestCase
{
    /*string(151) "/var/www/mercadoSabores/vendor/sensio/generator-bundle/Sensio/Bundle/GeneratorBundle/Command/../Resources/skeleton/crud/tests/others/short_scenario.php"
string(151) "/var/www/mercadoSabores/vendor/sensio/generator-bundle/Sensio/Bundle/GeneratorBundle/Command/../Resources/skeleton/crud/tests/others/short_scenario.php"

    public function testCompleteScenario()
    {
        // Create a new client to browse the application
        $client = static::createClient();

        // Go to the list view
        $crawler = $client->request('GET', '/admin/cidade/');
        $this->assertTrue(200 === $client->getResponse()->getStatusCode());

        // Go to the show view
        $crawler = $client->click($crawler->selectLink('show')->link());
        $this->assertTrue(200 === $client->getResponse()->getStatusCode());
    }
    */
}